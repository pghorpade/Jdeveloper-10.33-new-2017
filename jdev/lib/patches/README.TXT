This directory is where jar file patchsets for the product should be placed.

Only jar files that are directly in the lib/patches directory are
added to the IDE classpath at runtime. Jar files in subdirectories of
lib/patches will be ignored.
